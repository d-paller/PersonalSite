    <nav class="navbar navbar-default navbar-fixed-top">
        <div class="container-fluid">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <span class="navbar-brand">David Paller</span>
            </div>

            <div class="collapse navbar-collapse" id="myNavbar">
                <ul class="nav navbar-nav">
                    <li @(ViewBag.pageName == "Home" ? "class = \"active\"" : "")>
                        <a href="@Url.Action("Index", "Home")">Home</a>
                    </li>

                    <li @(ViewBag.pageName == "Resume" ? "class = \"active\"" : "")>
                        <a href="@Url.Action("Resume", "Home")">Resume</a>
                    </li>

                    <li @(ViewBag.pageName == "About" ? "class = \"active\"" : "")>
                        <a href="@Url.Action("About", "Home")">About</a>
                    </li>

                    <li @(ViewBag.pageName == "Contact" ? "class = \"active\"" : "")>
                        <a href="@Url.Action("Contact", "Home")">Contact</a>
                    </li>
                </ul>

                <div>
                    <a class="navbar-right" href="https://www.linkedin.com/in/davidpaller?trk=hp-identity-name" target="_blank">
                        <img class="social-icon" src="~/Content/LinkedInIcon.png" alt="Linked in icon" />
                    </a>
                </div>

                        
             </div>
        </div>
    </nav>